{
  "name": "pratik-bhuite.github.io",
  "version": "1.0.0",
  "description": "Personal portfolio website",
  "scripts": {
    "build": "node scripts/optimize-images.js && vite build",
    "dev": "vite",
    "preview": "vite preview",
    "deploy": "npm run build && cp -r dist/* . && git add . && git commit -m 'Deploy to GitHub Pages' && git push"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "cssnano": "^6.0.0",
    "imagemin": "^9.0.1",
    "imagemin-webp": "^8.0.0",
    "postcss": "^8.4.0",
    "sharp": "^0.34.5",
    "terser": "^5.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.4.0",
    "aos": "^2.3.4",
    "bootstrap": "^5.3.0"
  }
}
